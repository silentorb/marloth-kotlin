import silentorb.mythic.fathom.*
import silentorb.mythic.generation.texturing.RgbColor
import silentorb.mythic.spatial.*
import lib.*

let grassFloorOld = {
    let basicForm = squareFloor
    let detailedForm = basicForm . deform (dirtBump .* 0.2)
    let main = newModel detailedForm grassColor (collisionBox basicForm)
}
