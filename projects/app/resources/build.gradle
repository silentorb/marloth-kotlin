group 'resources'
version '1.0-SNAPSHOT'

defaultTasks 'copyDlls'

task copyDlls(type: Copy) {
    from '../../../../../cpp/cmake-build-debug/bin'
    into '../../../out/bin'
    include '*.dll'
    inputs.sourceFiles.stopExecutionIfEmpty()
}