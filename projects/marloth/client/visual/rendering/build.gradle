apply from: "$rootProject.projectDir/build_kotlin.gradle"

requires('platforming', 'spatial', 'scenery', 'sculpting', 'glowing', 'typography', 'drawing', 'texture_generation')

apply plugin: 'java'

//processResources()